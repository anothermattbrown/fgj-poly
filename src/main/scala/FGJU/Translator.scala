package FGJU {

  class Translator {
    // TODO: I think we need to typecheck to find where casts are needed for polymorphic.
    // Or, we can insert casts everywhere? Still need typechecking though.
    // if we have to typecheck anyway, may as well just program an interpreter ourselves.
    // why force ourselves to use Java's typechecker?
  }

}